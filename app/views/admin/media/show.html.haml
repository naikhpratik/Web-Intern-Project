.h2
  Media
  %small
    = @media.title
%br

.row
  .col-md-6
    = render partial: 'media_type', locals: { media: @media }

%br

.row
  .col-md-6
    .btn.btn-default.btn-sm
      = link_to('Back', admin_product_path(@product))
    .btn.btn-default.btn-sm
      = link_to('Edit', edit_admin_product_media_path(@product, @media))
