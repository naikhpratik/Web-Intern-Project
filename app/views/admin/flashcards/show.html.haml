.h2
  Flashcard
  %small
    = @flashcard.title
.help-block (Click on the tiles to flip sides)
- @flashcard.items.each_with_index do |item, index|
  .col-md-6
    .h4
      Flashcard
      = "##{index + 1}"
    .well.flashcard-item-view
      .h6
        .label
          FRONT
        = raw(item.front)
      .h6.hidden
        .label
          BACK
        = raw(item.back)
      .p.hidden
        Click the tile to flip sides

.row
  .col-md-6
    .btn.btn-default.btn-sm
      = link_to('Back', admin_product_path(@product))
    .btn.btn-default.btn-sm
      = link_to('Edit', edit_admin_product_flashcard_path(@product, @flashcard))
