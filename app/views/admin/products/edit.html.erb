<h3>Editing Product</h3>

<%= render 'form', product: @product %>

<%= link_to 'Show', admin_product_path(@product), class: 'btn btn-sm btn-info' %>
<%= link_to 'Back', admin_products_path, class: 'btn btn-sm btn-primary' %>
