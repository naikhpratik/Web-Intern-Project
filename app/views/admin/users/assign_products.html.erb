<%= form_for [:admin, @user], url: create_products_admin_user_path(@user), method: :post do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= render 'products', products: Product.all, user: @user, form: true %>
  <div class="actions">
    <%= f.submit 'Update User Products' %>
  </div>
<% end %>

<%= link_to 'Back', :back %>
