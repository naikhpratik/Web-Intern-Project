<h1>Dashboard#dashboard</h1>
<br>
<h4>List of products to show</h4>
<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td><%= product.name %> </td>
        <% if user_signed_in? %>
          <% if current_user.isadmin %>
            <td><%= link_to 'Show', product %></td>
            <td><%= link_to 'Edit', edit_product_path(product) %></td>
            <td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
            <td><%= link_to 'Manage Product', product %></td>
          <% end %>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<% if user_signed_in? %>
<h4>List of products of <%= current_user.username %></h4>
<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @userproducts.each do |userproduct| %>
      <tr>
            <% if current_user.id == userproduct.user_id %>
          <%= @products.find(userproduct.product_id).name %><br/>
        <% end %>
      </tr>
    <% end %>

  </tbody>
</table>
<br />

<% if user_signed_in? && current_user.isadmin? %>
<%= link_to 'New Product', new_product_path %>
<% end %>
<% end %>
